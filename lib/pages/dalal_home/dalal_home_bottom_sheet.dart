import 'package:flutter/material.dart';

class DalalHomeBottomSheet extends StatelessWidget {
  const DalalHomeBottomSheet({Key? key}) : super(key: key);

  @override
  build(context) => const SizedBox(
        height: 200,
        child: Text('Bottom sheet'),
      );
}
